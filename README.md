# Paper Extractor

This projects extracts papers information from google scholar and stores them in csv files

The resulted csv files are stored in `./datasets/` folder and partitionned over different files of 10000 papers max for a file

***

to start the extraction run 
`
python3 test.py
`
